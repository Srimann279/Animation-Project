# 🎨 Animation Project using HTML & CSS

This project demonstrates an **animation effect** created using **HTML** and **CSS**.  
The main focus is on the **rotation and color transitions** of a box, showing how CSS animations can make web pages more interactive and visually appealing.

---

## 🧠 **Project Overview**
- The animation is applied to a single box using the `@keyframes` rule in CSS.
- The box moves across the screen and changes its background color at different stages.
- The animation showcases multiple colors and smooth transitions.

---

## 🌈 **Colors Used**
- 🟩 Green  
- 🟦 Blue  
- 💖 Pink  
- ⚫ Black  
- 🟥 Red  

---

## ⚙️ **Animation Features**
| Property | Value | Description |
|-----------|--------|-------------|
| `animation-name` | `square` | Defines the name of the animation |
| `animation-duration` | `3s` | Runs for 3 seconds per cycle |
| `animation-timing-function` | `linear` | Moves smoothly from start to end |
| `animation-delay` | `0s` | Starts immediately |
| `animation-direction` | `alternate` | Reverses direction on each iteration |
| `animation-iteration-count` | `2` | Repeats the animation twice |

---

## 🧩 **Technologies Used**
- HTML5  
- CSS3 (Keyframes Animation)

---

## 🚀 **How to View**
1. Open the `index.html` file in your browser, **or**
2. Visit the live page (if GitHub Pages is enabled):

